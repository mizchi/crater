# Crater Network

WebDriver BiDi synthetic network state and byte/query encoding helpers shared
by Crater adapters.

This module is not a general HTTP client boundary. Browser HTTP policy stays in
`mizchi/crater-browser-http`; WebDriver BiDi command dispatch and browser
integration stay in `mizchi/crater-webdriver-bidi`.
