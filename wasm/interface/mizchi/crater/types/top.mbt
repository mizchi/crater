// Generated by `wit-bindgen` 0.47.0. DO NOT EDIT!

///|
/// Cache statistics
pub(all) struct CacheStats {
  hits : UInt
  misses : UInt
  nodes_computed : UInt
  hit_rate : Double
}

///|
/// Flex direction enum values
pub(all) enum FlexDirection {
  ROW
  ROW_REVERSE
  COLUMN
  COLUMN_REVERSE
}

///|
pub fn FlexDirection::ordinal(self : FlexDirection) -> Int {
  match self {
    ROW => 0
    ROW_REVERSE => 1
    COLUMN => 2
    COLUMN_REVERSE => 3
  }
}

///|
pub fn FlexDirection::from(self : Int) -> FlexDirection {
  match self {
    0 => ROW
    1 => ROW_REVERSE
    2 => COLUMN
    3 => COLUMN_REVERSE
    _ => panic()
  }
}

///|
/// Flex wrap enum values
pub(all) enum FlexWrap {
  NO_WRAP
  WRAP
  WRAP_REVERSE
}

///|
pub fn FlexWrap::ordinal(self : FlexWrap) -> Int {
  match self {
    NO_WRAP => 0
    WRAP => 1
    WRAP_REVERSE => 2
  }
}

///|
pub fn FlexWrap::from(self : Int) -> FlexWrap {
  match self {
    0 => NO_WRAP
    1 => WRAP
    2 => WRAP_REVERSE
    _ => panic()
  }
}

///|
/// Justify content enum values
pub(all) enum JustifyContent {
  FLEX_START
  FLEX_END
  CENTER
  SPACE_BETWEEN
  SPACE_AROUND
  SPACE_EVENLY
}

///|
pub fn JustifyContent::ordinal(self : JustifyContent) -> Int {
  match self {
    FLEX_START => 0
    FLEX_END => 1
    CENTER => 2
    SPACE_BETWEEN => 3
    SPACE_AROUND => 4
    SPACE_EVENLY => 5
  }
}

///|
pub fn JustifyContent::from(self : Int) -> JustifyContent {
  match self {
    0 => FLEX_START
    1 => FLEX_END
    2 => CENTER
    3 => SPACE_BETWEEN
    4 => SPACE_AROUND
    5 => SPACE_EVENLY
    _ => panic()
  }
}

///|
/// Align items enum values
pub(all) enum AlignItems {
  FLEX_START
  FLEX_END
  CENTER
  STRETCH
  BASELINE
}

///|
pub fn AlignItems::ordinal(self : AlignItems) -> Int {
  match self {
    FLEX_START => 0
    FLEX_END => 1
    CENTER => 2
    STRETCH => 3
    BASELINE => 4
  }
}

///|
pub fn AlignItems::from(self : Int) -> AlignItems {
  match self {
    0 => FLEX_START
    1 => FLEX_END
    2 => CENTER
    3 => STRETCH
    4 => BASELINE
    _ => panic()
  }
}

///|
/// Display enum values
pub(all) enum Display {
  FLEX
  NONE
  BLOCK
  GRID
}

///|
pub fn Display::ordinal(self : Display) -> Int {
  match self {
    FLEX => 0
    NONE => 1
    BLOCK => 2
    GRID => 3
  }
}

///|
pub fn Display::from(self : Int) -> Display {
  match self {
    0 => FLEX
    1 => NONE
    2 => BLOCK
    3 => GRID
    _ => panic()
  }
}
