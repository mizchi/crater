// Generated by `wit-bindgen` 0.47.0, modified to call crater implementation

///|
/// Create a new node
pub fn create_node(id : String) -> UInt {
  @crater.createNode(id).reinterpret_as_uint()
}

///|
/// Add child to parent
pub fn add_child(parent_id : String, child_id : String) -> Bool {
  @crater.addChild(parent_id, child_id)
}

///|
/// Insert child at index
pub fn insert_child(
  parent_id : String,
  child_id : String,
  index : UInt,
) -> Bool {
  @crater.insertChild(parent_id, child_id, index.reinterpret_as_int())
}

///|
/// Remove child at index
pub fn remove_child(parent_id : String, index : UInt) -> Bool {
  @crater.removeChild(parent_id, index.reinterpret_as_int())
}

///|
/// Get child count
pub fn get_child_count(node_id : String) -> UInt {
  @crater.getChildCount(node_id).reinterpret_as_uint()
}

///|
/// Set width in pixels
pub fn set_width(node_id : String, value : Double) -> Bool {
  @crater.setWidth(node_id, value)
}

///|
/// Set width as percentage (0-100)
pub fn set_width_percent(node_id : String, value : Double) -> Bool {
  @crater.setWidthPercent(node_id, value)
}

///|
/// Set width to auto
pub fn set_width_auto(node_id : String) -> Bool {
  @crater.setWidthAuto(node_id)
}

///|
/// Set height in pixels
pub fn set_height(node_id : String, value : Double) -> Bool {
  @crater.setHeight(node_id, value)
}

///|
/// Set height as percentage (0-100)
pub fn set_height_percent(node_id : String, value : Double) -> Bool {
  @crater.setHeightPercent(node_id, value)
}

///|
/// Set height to auto
pub fn set_height_auto(node_id : String) -> Bool {
  @crater.setHeightAuto(node_id)
}

///|
/// Set flex grow
pub fn set_flex_grow(node_id : String, value : Double) -> Bool {
  @crater.setFlexGrow(node_id, value)
}

///|
/// Set flex shrink
pub fn set_flex_shrink(node_id : String, value : Double) -> Bool {
  @crater.setFlexShrink(node_id, value)
}

///|
/// Set flex basis in pixels
pub fn set_flex_basis(node_id : String, value : Double) -> Bool {
  @crater.setFlexBasis(node_id, value)
}

///|
/// Set flex direction
pub fn set_flex_direction(
  node_id : String,
  value : @types.FlexDirection,
) -> Bool {
  @crater.setFlexDirection(node_id, value.ordinal())
}

///|
/// Set flex wrap
pub fn set_flex_wrap(node_id : String, value : @types.FlexWrap) -> Bool {
  @crater.setFlexWrap(node_id, value.ordinal())
}

///|
/// Set justify content
pub fn set_justify_content(
  node_id : String,
  value : @types.JustifyContent,
) -> Bool {
  @crater.setJustifyContent(node_id, value.ordinal())
}

///|
/// Set align items
pub fn set_align_items(node_id : String, value : @types.AlignItems) -> Bool {
  @crater.setAlignItems(node_id, value.ordinal())
}

///|
/// Set display
pub fn set_display(node_id : String, value : @types.Display) -> Bool {
  @crater.setDisplay(node_id, value.ordinal())
}

///|
/// Set margin on all sides
pub fn set_margin(node_id : String, value : Double) -> Bool {
  @crater.setMargin(node_id, value)
}

///|
/// Set padding on all sides
pub fn set_padding(node_id : String, value : Double) -> Bool {
  @crater.setPadding(node_id, value)
}

///|
/// Set gap
pub fn set_gap(node_id : String, value : Double) -> Bool {
  @crater.setGap(node_id, value)
}

///|
/// Get computed left (x)
pub fn get_computed_left(node_id : String) -> Double {
  @crater.getComputedLeft(node_id)
}

///|
/// Get computed top (y)
pub fn get_computed_top(node_id : String) -> Double {
  @crater.getComputedTop(node_id)
}

///|
/// Get computed width
pub fn get_computed_width(node_id : String) -> Double {
  @crater.getComputedWidth(node_id)
}

///|
/// Get computed height
pub fn get_computed_height(node_id : String) -> Double {
  @crater.getComputedHeight(node_id)
}

///|
/// Check if node has new layout
pub fn has_new_layout(node_id : String) -> Bool {
  @crater.hasNewLayout(node_id)
}

///|
/// Mark layout as seen
pub fn mark_layout_seen(node_id : String) -> Bool {
  @crater.markLayoutSeen(node_id)
}

///|
/// Calculate layout (returns structured data)
pub fn calculate_layout_structured(
  width : Double,
  height : Double,
) -> @crater.FlatLayoutResult {
  @crater.calculateLayoutStructured(width, height)
}

///|
/// Calculate layout (returns JSON)
pub fn calculate_layout(width : Double, height : Double) -> String {
  @crater.calculateLayout(width, height)
}
