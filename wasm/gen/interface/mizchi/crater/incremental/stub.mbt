// Generated by `wit-bindgen` 0.47.0, modified to call crater implementation

///|
/// Create a new layout tree from HTML
pub fn create_tree(html : String, width : UInt, height : UInt) -> UInt {
  @crater.createTree(html, width.reinterpret_as_int(), height.reinterpret_as_int()).reinterpret_as_uint()
}

///|
/// Compute layout incrementally (uses cache, returns structured data)
pub fn compute() -> @crater.FlatLayoutResult {
  @crater.computeStructured()
}

///|
/// Compute layout incrementally (uses cache) - legacy JSON version
pub fn compute_incremental() -> String {
  @crater.computeIncremental()
}

///|
/// Compute full layout (ignores cache, returns structured data)
pub fn compute_full_structured() -> @crater.FlatLayoutResult {
  @crater.computeFullStructured()
}

///|
/// Compute full layout (ignores cache)
pub fn compute_full() -> String {
  @crater.computeFull()
}

///|
/// Mark a node as dirty
pub fn mark_dirty(node_id : String) -> Bool {
  @crater.markDirty(node_id)
}

///|
/// Update node style with CSS string
pub fn update_style(node_id : String, css : String) -> Bool {
  @crater.updateStyle(node_id, css)
}

///|
/// Resize viewport
pub fn resize_viewport(width : UInt, height : UInt) -> Unit {
  @crater.resizeViewport(width.reinterpret_as_int(), height.reinterpret_as_int())
}

///|
/// Get cache statistics as JSON
pub fn get_cache_stats() -> String {
  @crater.getCacheStats()
}

///|
/// Reset cache statistics
pub fn reset_cache_stats() -> Unit {
  @crater.resetCacheStats()
}

///|
/// Check if tree needs layout
pub fn needs_layout() -> Bool {
  @crater.needsLayout()
}

///|
/// Destroy the current tree
pub fn destroy_tree() -> Unit {
  @crater.destroyTree()
}
