MODULE=css-contain
BASELINE_TOTAL=303
BASELINE_PASSED=281
BASELINE_FAILED=22
BASELINE_UPDATED_AT=2026-06-29T00:00:00Z
NOTE="Pinned after the Tinos font-metric text measurement landed (PR #267), which lifted css-contain from 274 to 282 passing on the wpt.json gradeable subset. Realigned 282/21 -> 281/22 on 2026-06-29: one non-text containment-sizing fixture regressed to failing. This is a pre-existing residual on main (reproduces identically on the merge-base 7749b67c and on PR #321's head), NOT introduced by the incremental-reflow work — the WPT runner renders through the static compute_layout path, which the block-flow memoization leaves untouched (it reads/writes its cache only while an incremental reflow is active). The 22 failures are non-text layout residuals tracked as follow-up; identifying and fixing the specific containment-sizing fixture is a separate task."
