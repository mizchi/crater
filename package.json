{
  "name": "crater",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "gentest": "node --experimental-strip-types tools/gentest.ts",
    "test": "vitest"
  },
  "dependencies": {
    "puppeteer": "^23.0.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
